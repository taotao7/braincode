# Demo Agent Instructions

- Use Bun for package scripts.
- Keep login validation behavior in `src/login.ts`.
- Preserve the `LoginResult` return shape.
- Run `bun test ./src/login.bench.ts` before reporting completion.
