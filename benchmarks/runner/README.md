# Execution Benchmark Runner

The executable runner lives in `packages/agent-runtime/src/execution-benchmark.ts` so the CLI and tests can share it.

Use:

```bash
braincode benchmark --execute
braincode benchmark --execute --task login-validation
braincode benchmark --execute --real --json
```
